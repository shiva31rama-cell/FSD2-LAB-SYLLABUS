# MongoDB Databases, Collections and Records

## Syllabus tasks
- Create and drop databases and collections.
- Work with records using `find()`, `limit()`, `sort()`, `createIndex()` and `aggregate()`.

## Run
Use `mongosh` and execute `queries.js`.
