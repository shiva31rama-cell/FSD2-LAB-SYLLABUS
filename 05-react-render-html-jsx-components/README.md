# ReactJS Render HTML, JSX, Function and Class Components

## Syllabus tasks
- Render HTML to a web page.
- Write markup with JSX.
- Create and nest function and class components.

## Run
From the repository root: `npm run react05` → open the URL shown by Vite.
